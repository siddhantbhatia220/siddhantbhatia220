name: LeetCode Stats Sync
on:
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Ready
        run: echo "LeetCode analytics are dynamically embedded in README via SVG."
